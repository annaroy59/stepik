## Арифметические строки
## Вводятся 3 строки в случайном порядке.
## Напишите программу, которая выясняет, можно ли из длин этих строк построить арифметическую прогрессию.

## Формат входных данных
## На вход программе подаются три строки, каждая на отдельной строке.

## Формат выходных данных
##Программа должна вывести строку «YES», если из длин введенных слов можно построить арифметическую прогрессию, или «NO» - в противном случае.

name1 = input()
name2 = input()
name3 = input()
length1 = len(name1)
length2 = len(name2)
length3 = len(name3)
s = length1+length2+length3
mn = min(length1, length2, length3)
mx = max(length1, length2, length3)
sr = s-mn-mx
if mx-sr == sr-mn:
    print('YES')
else:
    print('NO')
